const assert = require('assert');
const minasonWifi = require('../index.js');

describe('minasonWifi', () => {
  it('has a test', () => {
    assert(false, 'minasonWifi should have a test');
  });
});
